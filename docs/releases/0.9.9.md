# sarmg-foundation-client v0.9.9

- 为配对入口不存在、HTTP 方法被拒绝、服务端需升级、请求被拒绝及意外 HTTP 状态提供独立公共错误文案。
- 管理权限提示统一覆盖 Windows administrator 与 Unix root，避免 macOS/Linux 显示 Windows 专属文案。
- macOS PTY 回归测试按 Darwin 的 `POLLHUP` 行为非阻塞排空子进程最终输出。
- 保留原生受保护终端输入、绝对截止时间、字段长度上限、取消处理和终端状态恢复。
