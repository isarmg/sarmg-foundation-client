# Foundation Client 0.9.7

The Unix pseudo-terminal regression harness now uses the portable mutable
`openpty` termios and window-size pointer signature required by macOS. This
restores native macOS CI coverage for secret input, cancellation, timeout,
length limits and terminal echo restoration.

The production native terminal implementation and Windows Console event loop
remain the same as 0.9.6.
