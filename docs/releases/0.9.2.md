# Foundation Client 0.9.2

Secure HTTP now preserves stable, source-free TCP and TLS failure categories in
addition to its existing DNS, address-policy, timeout and response-budget
categories. Backend error chains, request URLs and credentials never escape the
Foundation boundary. Connection-refusal coverage prevents future regressions
back to the former combined transport error.
