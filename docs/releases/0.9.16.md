# sarmg-foundation-client v0.9.16

- 命令行输出对字段名执行转义，并在结构化数据及混合大小写字段中隐藏秘密值，避免终端显示或日志泄露凭据。
- 编辑配置和读取 macOS 日志采用明确的大小上限，超限时返回稳定错误，不继续分配无限内存。

全部八个 crate 使用统一的精确版本。消费者应同时固定 `=0.9.16` 和本次发布对应的完整 Git revision。
