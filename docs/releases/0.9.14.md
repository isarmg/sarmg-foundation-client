# sarmg-foundation-client v0.9.14

- Windows 交互式 Setup 的 UAC 父进程不再无限等待；等待时间受命令 `--timeout` 的绝对截止时间约束，并以稳定的 `elevated_setup_timeout` 错误结束。
- 提权前明确提示用户切换到管理员控制台，提权子进程明确说明秘密输入不会回显，减少在错误窗口输入或误判卡死的情况。
- 不通过命令行传递授权码或其他秘密；现有原生 Windows Console 输入、取消、长度限制和控制台模式恢复边界保持不变。
