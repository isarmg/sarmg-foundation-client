# Foundation Client 0.9.6

Windows interactive input now consumes native Console input records instead of
calling a character read after a wait. Mouse, resize and focus events are
ignored without entering a blocking read, so the absolute input deadline remains
effective even when unrelated Console events are queued.

All bounded native terminal behavior introduced in 0.9.5 remains unchanged.
