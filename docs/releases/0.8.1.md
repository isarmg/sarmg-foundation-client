# 0.8.1

This patch republishes the current Client Foundation security and recovery baseline under a new immutable version. Public HTTPS address admission, explicit TLS trust modes, pinned client reuse, Windows-safe storage names, bounded object bindings, and deterministic recovery scheduling are unchanged from 0.8.0.

The complete Rust workspace is rebuilt and verified on Linux, Windows, and macOS from the tagged source.
