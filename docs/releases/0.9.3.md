# Foundation Client 0.9.3

The shared secure HTTP and DNS policy layer has been removed. Public-network
transport is now owned by each product adapter so platform proxy, DNS, VPN and
TLS behavior follows the product's standard HTTP client.

Foundation continues to provide lifecycle, filesystem, secret, CLI and mobile
runtime primitives. Product-specific HTTPS validation and response budgets no
longer create a shared network-policy dependency.
