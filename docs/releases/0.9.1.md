# 0.9.1

- 人类输出改为有界、可操作的字段摘要，JSON/NDJSON 机器合同保持独立。
- 重复 Setup 默认保留操作系统服务现有的启用和运行意图；业务就绪失败明确保留已提交安装状态。
- 新增由产品 revision 校验和原子提交包围的终端 JSON 编辑器入口。
- Linux、Windows 与 macOS CI 都显式编译、测试并 lint `sarmg-client-cli`。
- Client Profile 校验不再依赖 Foundation 内置的产品名称清单。
